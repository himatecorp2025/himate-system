databases:
  - name: himate-db
    databaseName: himate
    user: himate

services:
  - type: pserv
    name: himate-catalog
    runtime: docker
    dockerfilePath: ./services/docker/catalog.Dockerfile
    dockerContext: .
    autoDeploy: false
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: himate-db
          property: connectionString
      - key: HIMATE_INTERNAL_TOKEN
        generateValue: true

  - type: pserv
    name: himate-partners
    runtime: docker
    dockerfilePath: ./services/docker/partners.Dockerfile
    dockerContext: .
    autoDeploy: false
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: himate-db
          property: connectionString
      - key: HIMATE_INTERNAL_TOKEN
        fromService:
          type: pserv
          name: himate-catalog
          envVarKey: HIMATE_INTERNAL_TOKEN

  - type: pserv
    name: himate-billing
    runtime: docker
    dockerfilePath: ./services/docker/billing.Dockerfile
    dockerContext: .
    autoDeploy: false
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: himate-db
          property: connectionString
      - key: HIMATE_INTERNAL_TOKEN
        fromService:
          type: pserv
          name: himate-catalog
          envVarKey: HIMATE_INTERNAL_TOKEN
      - key: CATALOG_HOSTPORT
        fromService:
          type: pserv
          name: himate-catalog
          property: hostport

  - type: pserv
    name: himate-contact
    runtime: docker
    dockerfilePath: ./services/docker/contact.Dockerfile
    dockerContext: .
    autoDeploy: false
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: himate-db
          property: connectionString
      - key: HIMATE_INTERNAL_TOKEN
        fromService:
          type: pserv
          name: himate-catalog
          envVarKey: HIMATE_INTERNAL_TOKEN
      - key: HIMATE_CONTACT_NOTIFY_TO
        sync: false
      - key: SMTP_HOST
        sync: false
      - key: SMTP_PORT
        value: "587"
      - key: SMTP_USERNAME
        sync: false
      - key: SMTP_PASSWORD
        sync: false
      - key: SMTP_FROM
        sync: false

  - type: web
    name: himate
    runtime: docker
    dockerfilePath: ./services/docker/gateway.Dockerfile
    dockerContext: .
    healthCheckPath: /api/v1/health
    renderSubdomainPolicy: enabled
    autoDeploy: false
    envVars:
      - key: HIMATE_ENV
        value: production
      - key: HIMATE_APP_VERSION
        value: 0.2.2-brandbook-contact-performance
      - key: COOKIE_SECURE
        value: "true"
      - key: HIMATE_SESSION_TTL_HOURS
        value: "8"
      - key: HIMATE_SESSION_SECRET
        generateValue: true
      - key: HIMATE_BOOTSTRAP_ADMIN_NAME
        sync: false
      - key: HIMATE_BOOTSTRAP_ADMIN_EMAIL
        sync: false
      - key: HIMATE_BOOTSTRAP_ADMIN_PASSWORD
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: himate-db
          property: connectionString
      - key: HIMATE_INTERNAL_TOKEN
        fromService:
          type: pserv
          name: himate-catalog
          envVarKey: HIMATE_INTERNAL_TOKEN
      - key: PARTNERS_HOSTPORT
        fromService:
          type: pserv
          name: himate-partners
          property: hostport
      - key: CATALOG_HOSTPORT
        fromService:
          type: pserv
          name: himate-catalog
          property: hostport
      - key: BILLING_HOSTPORT
        fromService:
          type: pserv
          name: himate-billing
          property: hostport
      - key: CONTACT_HOSTPORT
        fromService:
          type: pserv
          name: himate-contact
          property: hostport

  - type: cron
    name: himate-30day-invoice-cycle
    runtime: docker
    schedule: "0 6 * * *"
    dockerfilePath: ./services/docker/billing.Dockerfile
    dockerContext: .
    dockerCommand: /app/service --run-invoice-cycle
    autoDeploy: false
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: himate-db
          property: connectionString
      - key: HIMATE_INTERNAL_TOKEN
        fromService:
          type: pserv
          name: himate-catalog
          envVarKey: HIMATE_INTERNAL_TOKEN
      - key: CATALOG_HOSTPORT
        fromService:
          type: pserv
          name: himate-catalog
          property: hostport
